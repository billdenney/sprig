# Spike: swift-cross-ui on Windows (R1/R16)

Standalone package — NOT product code, NOT in the root manifest's graph. Run on the
Windows VM from `C:\sprig\spikes\swift-cross-ui` with `swift build`. Findings land in
`docs/research/swift-cross-ui-spike-2026-06.md` and the risk register (R1, R16).
