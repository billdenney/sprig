// GitCore — portable (Tier 1) package. No UI, no platform APIs.
// Must compile on macOS, Linux, and Windows Swift 6.3.
// See CLAUDE.md and ADR 0048 for cross-platform discipline.

import Foundation

public enum GitCore {
    public static let moduleName = "GitCore"
}
