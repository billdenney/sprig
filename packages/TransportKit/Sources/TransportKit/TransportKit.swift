// TransportKit — adapter (Tier 2) package.
// The protocol lives here (portable); implementations live in
// Sources/Mac, Sources/Linux, Sources/Windows.
// See ADR 0048 and CLAUDE.md.

import Foundation
import PlatformKit

public enum TransportKit {
    public static let moduleName = "TransportKit"
}
