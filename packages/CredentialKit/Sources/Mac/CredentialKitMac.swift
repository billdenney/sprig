#if os(macOS)
    import Foundation

    // macOS implementation stub — see ADR 0048 / CLAUDE.md.
    // Real impl arrives in the relevant milestone (see docs/planning/roadmap.md).

    enum CredentialKitMacImpl {
        static let platform = "macOS"
    }
#endif
