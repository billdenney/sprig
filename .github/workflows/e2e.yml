name: E2E (XCUITest)
on:
  pull_request:
    paths:
      - 'apps/macos/**'
      - 'packages/**'
  workflow_dispatch:

jobs:
  e2e:
    runs-on: [self-hosted, macOS, arm64]
    steps:
      - uses: actions/checkout@v6
      - run: ./script/test --suite=e2e
